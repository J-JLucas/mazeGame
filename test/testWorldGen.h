/* Tests the world generation process from start to finish.
 * starts with populating the map grid
 * then cuts out the openings through the walls to create the maze
 * finish by rending the map to the console
 */

#pragma once

#include <vector>

std::vector<float> testWorldGeneration();
